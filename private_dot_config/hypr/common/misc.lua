hl.config({
    misc = {
        force_default_wallpaper = 0,
        disable_hyprland_logo = true,
        -- Lets a restarted quickshell reclaim the session lock if it crashed
        -- while locked, instead of leaving the "lockscreen died" screen.
        allow_session_lock_restore = true,
    },
})
